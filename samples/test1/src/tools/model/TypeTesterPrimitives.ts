/** Primitives: string, number, boolean. */
export interface TypeTesterPrimitives {
  /** A string value. */
  text: string;
  /** A number value. */
  count: number;
  /** A boolean value. */
  flag: boolean;
}
