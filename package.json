{
  "name": "plugin",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.13.1"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "build:libs": "pnpm --filter @quartal/plugin-core --filter @quartal/plugin run build",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck"
  }
}
