/** Custom type for multiplying two numbers. */
export interface MultiplyThese {
  /**
   * The first number.
   * @example 5
   */
  first: number;
  /**
   * The second number.
   * @example 3
   */
  second: number;
}
