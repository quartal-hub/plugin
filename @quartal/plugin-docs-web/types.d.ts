/**
 * Hand-maintained types for the built library (`dist/plugin-docs-web.js`); the Vite lib build
 * emits no declarations. Keep in sync with `src/mount.ts`.
 */

/** Mounts the plugin docs UI into `el`. See `src/mount.ts`. */
export declare function mountPluginDocs(el: Element): void;
