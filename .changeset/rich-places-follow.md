---
---

Deployment scripts for the sample plugins (`deployment/`), plus the root `deploy-plugin` script,
`AGENTS.md` and the eslint ignore for generated stages. No published package changed, so this
release carries no version bump — see `deployment/README.md` for the tooling.
